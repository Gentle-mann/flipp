class User {
  final String name, role, profileImageUrl;
  final bool darkMode;

  User({
    required this.name,
    required this.role,
    required this.profileImageUrl,
    required this.darkMode,
  });
}
