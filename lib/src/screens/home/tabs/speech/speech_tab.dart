import 'package:flutter/material.dart';

import '../../../../theme/themes_barrel.dart';

class SpeechTab extends StatelessWidget {
  const SpeechTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Coming Soon...',
        style: LightTextTheme.lightTextTheme.displayMedium,
      ),
    );
  }
}
